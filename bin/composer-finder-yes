#!/usr/bin/env ruby
# this requires the composer_finder_yes file to the CLI file.
# It creates a new instance of the CLI for the user to interact with.

require './lib/composer_finder_yes'

ComposerFinderYes::CLI.new.call