#!/usr/bin/env ruby
# above is the Interpreter: This tells the program to use Ruby.
# below: This creates a new instance of the CLI for the user to interact with and instantiates a new CLI.

require './lib/environment'

ComposerFinder::CLI.new.call