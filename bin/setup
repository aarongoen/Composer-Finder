#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'
set -vx

# Installs the bundler. Bundler is a package that sets up a template of necessary files.

bundle install

# Do any other automated setup that you need to do here
